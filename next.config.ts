/** @type {import('next').NextConfig} */
const nextConfig = {
  // images: {
  //   domains: ["localhost"], // Update this with your production domain if needed
  //   deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048],
  //   imageSizes: [16, 32, 64, 96, 128, 256, 384],
  //   formats: ["image/webp"], // Use WebP for better compression
  //   minimumCacheTTL: 60 * 60 * 24 * 7, // Cache for 7 days
  // },
  // experimental: {
  //   optimizeCss: true, // Optional: optimize CSS
  // },
};

module.exports = nextConfig;
